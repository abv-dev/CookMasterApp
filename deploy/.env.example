# MongoDB Atlas Connection String (pour API serverless)
# Remplacez par votre propre chaîne de connexion MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cookmaster?retryWrites=true&w=majority

# JWT Secret (pour l'authentification future)
# Utilisez une clé secrète forte et unique en production
JWT_SECRET=your-secret-key-here
